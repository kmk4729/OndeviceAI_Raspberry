# custom_model/

사용자가 직접 설계하는 얼굴 인식 모델을 위한 작업 공간입니다.
기존 `train/`, `deploy/`, `utils/`의 스크립트와 분리되어 있으니, 이 폴더 안에서 자유롭게 구조를 잡아 실험하면 됩니다.

완성 후 배포 파이프라인에 연결할 때는:
- 학습 스크립트는 `train/`으로
- Pi에서 실행할 추론 스크립트는 `deploy/`로
옮기는 것을 권장합니다.
